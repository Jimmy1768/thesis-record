# Templates

Reusable ThesisRecord scaffolding lives here.

Templates are platform-level guidance. Copy or adapt them into
`theses/<slug>/` when creating a new thesis, then keep thesis-specific content
inside the thesis folder.
