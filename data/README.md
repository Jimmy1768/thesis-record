# Platform Data

Root `data/` is reserved for platform-level templates or shared non-thesis
fixtures.

Thesis-specific source manifests, raw extracts, intermediate data, processed
data, and analysis outputs belong under `theses/<slug>/evidence/`.
